//Source code generated by AppGPT (www.appgpt.tech)
import {
  Datagrid,
  List,
  EditButton,
  Edit,
  SimpleForm,
  Create,
  SelectColumnsButton,
  DatagridConfigurable,
  TopToolbar,
  CreateButton,
  ExportButton,
  FilterButton,
  //Field controls
  BooleanField,
  DateField,
  EmailField,
  ImageField,
  NumberField,
  ReferenceField,
  TextField,
  UrlField,
  //Input controls
  BooleanInput,
  DateInput,
  EmailInput,
  ImageInput,
  NumberInput,
  ReferenceInput,
  TextInput,
  UrlInput,
  PasswordInput,
} from 'react-admin';
import { useRecordContext } from 'react-admin';
import { Grid } from '@mui/material';
const ReadOnlyPasswordField = ({ record, source }) => {
  // You can customize the way you display the password here, e.g., mask it with asterisks
  const maskedPassword = '********';

  return <span>{maskedPassword}</span>;
};
const ListActions = () => (
  <TopToolbar>
    <FilterButton />
    <CreateButton />
    <ExportButton />
    <SelectColumnsButton />
  </TopToolbar>
);
const messagesTitle = () => {
  const record = useRecordContext();
  return <span>messages {record ? `"${record.MessageContent}"` : ''}</span>;
};

export const messagesList = () => (
  <List actions={<ListActions />} filters={ResourceFilters}>
    <DatagridConfigurable>
      <ReferenceField source="Sender" reference="users" />
      <ReferenceField source="Receiver" reference="users" />
      <TextField source="MessageContent" />
      <DateField source="Timestamp" />
      <EditButton />
    </DatagridConfigurable>
  </List>
);

export const messagesEdit = () => (
  <Edit title={<messagesTitle />}>
    <SimpleForm>
      <Grid container rowSpacing={1} columnSpacing={{ xs: 1, sm: 2, md: 5 }}>
        <Grid item xs={4}>
          <ReferenceInput source="Sender" reference="users" />
        </Grid>
        <Grid item xs={4}>
          <ReferenceInput source="Receiver" reference="users" />
        </Grid>
        <Grid item xs={4}>
          <TextInput source="MessageContent" />
        </Grid>
        <Grid item xs={4}>
          <DateInput source="Timestamp" />
        </Grid>
      </Grid>
    </SimpleForm>
  </Edit>
);

export const messagesCreate = () => (
  <Create>
    <SimpleForm>
      <Grid container rowSpacing={1} columnSpacing={{ xs: 1, sm: 2, md: 5 }}>
        <Grid item xs={4}>
          <ReferenceInput source="Sender" reference="users" />
        </Grid>
        <Grid item xs={4}>
          <ReferenceInput source="Receiver" reference="users" />
        </Grid>
        <Grid item xs={4}>
          <TextInput source="MessageContent" />
        </Grid>
        <Grid item xs={4}>
          <DateInput source="Timestamp" />
        </Grid>
      </Grid>
    </SimpleForm>
  </Create>
);

const ResourceFilters = [
  <TextInput source="q" label="Search" alwaysOn />,
  <ReferenceInput source="Sender" label="Sender" reference="users" alwaysOn />,
  <ReferenceInput
    source="Receiver"
    label="Receiver"
    reference="users"
    alwaysOn
  />,
  ,
  ,
];
